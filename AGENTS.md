# Repository Agent Rules

## Versioning
- For every commit, bump the project version in that same commit.
- Minor fixes/chore-level changes: bump by `0.0.1`.
- Large functionality changes: bump by `0.1.0`.
- If this repo uses multiple version manifests, keep all of them in sync in the same commit.
